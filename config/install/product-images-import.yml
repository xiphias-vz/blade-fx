command-timeout: 14400

sections:
    download-csv-files:
        download:
            command: "vendor/bin/console data:import:files-download"

    import:
        import-images:
            command: "vendor/bin/console data:import:images-to-s3 -f spryker/2.globus_articles_images.zip"
