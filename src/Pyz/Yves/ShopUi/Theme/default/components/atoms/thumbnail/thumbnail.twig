{% extends atom('thumbnail', '@SprykerShop:ShopUi') %}

{% block body %}
    {% set imageExtraClasses = '' %}
    {% block image %}
        <img class="{{ component.renderClass(config.name ~ '__image', modifiers) }} {{ imageExtraClasses }}" {{ component.renderAttributes(attributes) }}>
    {% endblock %}
{% endblock %}
