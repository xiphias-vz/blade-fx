{% extends model('component') %}

{% define config = {
    name: 'navigation-inline-links-list'
} %}

{% define data = {
    nodes: [],

} %}

{% block body %}
    <ul id="unorderedListId" class="{{ component.renderClass(config.name, modifiers) }}">
        {% for node in data.nodes %}
            {% embed atom('node') with {
                modifiers: modifiers,
                class: 'list__item' ~ (node.url ? '' : ' ' ~ node.cssClass),
                config: {
                    tag: 'li'
                },
                data: {
                    node: node
                }
            } only %}
                {% block url %}
                    <div>
                        <a class="{{ component.renderClass(config.name ~ '__link', modifiers) }} {{ data.node.cssClass }}" href="{{ data.node.url }}">{{ data.node.title }}</a>
                    </div>
                {% endblock %}

                {% block externalUrl %}
                    <div>
                        <a class="{{ component.renderClass(config.name ~ '__link', modifiers) }} {{ data.node.cssClass }}" href="{{ data.node.url }}" target="_blank">{{ data.node.title }}</a>
                    </div>
                {% endblock %}

                {% block other %}
                    {{ data.node.title }}
                {% endblock %}
            {% endembed %}
        {% endfor %}
    </ul>

{% endblock %}


