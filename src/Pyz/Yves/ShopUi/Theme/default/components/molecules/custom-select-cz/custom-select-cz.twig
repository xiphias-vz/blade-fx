{% extends atom('select') %}

{% define config = {
    name: 'custom-select-cz',
    tag: 'custom-select-cz'
} %}

{% define attributes = {
    'config-width': 'resolve',
    'config-theme': 'default',
    'auto-init': false
} %}

{% block selectClass %}{{ config.jsName }}{% endblock %}

