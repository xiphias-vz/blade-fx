{% extends atom('select-edit') %}

{% define config = {
    name: 'custom-select',
    tag: 'custom-select'
} %}

{% define attributes = {
    'config-width': 'resolve',
    'config-theme': 'default',
    'auto-init': false
} %}

{% block selectClass %}{{ config.jsName }}{% endblock %}

