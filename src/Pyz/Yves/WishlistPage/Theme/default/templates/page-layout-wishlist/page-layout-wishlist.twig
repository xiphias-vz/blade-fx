{% extends template('page-layout-customer', 'CustomerPage') %}

{% define data = {
    wishlists: required,
    title: 'customer.account.wishlist' | trans,
    activePage: 'wishlist',
    activeWishlistId: null,
} %}

{% block customerNavigationContainer %}
    {% set customerNavigationData = [data.activePage, data.activeWishlistId] %}
    {{ parent() }}
{% endblock %}
