{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {{ form_row(form.idRecommendationScenarios) }}
    {{ form_row(form.scenarioId) }}
    {{ form_row(form.hashId) }}

        <input type="submit" class="btn btn-primary safe-submit" value="{{ 'Save' | trans }} " />
{{ form_end(form) }}

