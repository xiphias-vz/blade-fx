{% extends model('component') %}

{% define config = {
    name: 'order-articles-with-colors',
} %}

{% define data = {
    orderNumber: '',
    articles: []
} %}

{% block body %}
    <div class="{{ config.name }}__display-flex">

        {% for orderItemArticle in data.articles %}
            <div class="{{ config.name }}__circle-wrapper">
                <div class="{{ config.name }}__circle-spacing">
                    {% include atom('circle') with {
                        data: {
                            content: orderItemArticle.pickingColor.idColor,
                            backgroundColor: orderItemArticle.pickingColor.circleColor,
                            textColor: orderItemArticle.pickingColor.backColor,
                            paddingLeft: '',
                            paddingTop: '0.2rem',
                        }
                    } %}
                    <div class="{{ config.name }}__article-quantity">{{ orderItemArticle.quantity }}</div>
                </div>
            </div>


        {% endfor %}

    </div>
{% endblock body %}
